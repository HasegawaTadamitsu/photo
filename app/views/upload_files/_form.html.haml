= form_for @upload_file , :html => { :multipart => true }do |f|
  -if @upload_file.errors.any?
    #error_explanation
      %h2= "下の原因でアップロードできませんでした。"
      %ul
        - @upload_file.errors.full_messages.each do |msg|
          %li= msg

  .field
    = f.label :upload_file_name
    = f.file_field :upload_file_name
  .actions
    = f.submit 'upload'
