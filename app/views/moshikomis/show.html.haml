%h1
  = @data[:title]

= @data[:body]

.datas
  -  datas = @data[:upload_files]
  -  datas.each do |data|
    .data
      .pictute
        -    url     = @data[:base_pic_url] + data.url_without_basepath
        -    comment =  data.comment
        = image_tag url, :alt=>''
      .comment
        = comment


.text
  依頼主から
  = @data[:upload_datetime]
  に画像のアップロードがありました。
  - if @data[:access_count] != 0
    あなたをのぞき、のべ
    = @data[:access_count]
    人の方が閲覧しており、 最後にアクセスしたのは、
    = @data[:last_access_datetime]
    です。
  予定では
  = @data[:will_delete_datetime]
  に、非公開になる予定です。

  = link_to "このURLをメール送信する", "mailto:?body=#{@data[:url]}"
  = raw '<a href="http://twitter.com/share" class="twitter-share-button" data-url="' + @data[:url] + '" data-count="none" data-lang="ja">Tweet</a><script type="text/javascript" src="http://platform.twitter.com/widgets.js"></script>'
